import React from 'react';
import Header from 'components/Header';

const ListPage = () => {
    return (
        <div>
            <Header />
            List
        </div>
    );
};

export default ListPage;